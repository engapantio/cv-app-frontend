mutation UpdateProject($project: UpdateProjectInput!) {
  updateProject(project: $project) {
    id
    created_at
    name
    internal_name
    domain
    start_date
    end_date
    description
    environment
  }
}
